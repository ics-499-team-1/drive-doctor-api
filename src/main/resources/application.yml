server:
  servlet:
    context-path: /drive-doctor

spring:
  datasource:
    url: jdbc:postgresql://drive-doctor.c02zvzfbrnh9.us-east-1.rds.amazonaws.com:5432/drive-doctor
    username: postgres
    password: 1*1SivcrpL7m6tgLpsX$o{Hb9a3k
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    property-naming-strategy: SNAKE_CASE

